# ClipFlow 项目说明

## 1. 这个项目解决什么问题

ClipFlow 是一个 macOS 菜单栏剪贴板历史管理工具，主要解决日常复制内容容易丢失、难以回找、重复复制效率低的问题。

普通系统剪贴板通常只能保存最近一次复制内容。实际使用中，经常会遇到这些问题：

- 刚复制过的链接、代码、邮件、文字被新的复制内容覆盖。
- 想找之前复制过的内容，只能回原网页、聊天记录或文件里重新搜索。
- 剪贴板内容混在一起，没有分类，查找成本高。
- 剪贴板内容可能包含隐私信息，不适合交给云端服务处理。
- 长文本需要快速判断含义，但每次手动阅读很浪费时间。

ClipFlow 的核心目标，就是把“临时剪贴板”变成“可搜索、可分类、可收藏、可本地保存的工作流记录”。

## 2. 项目做了什么

项目已经实现了一个可运行的 macOS 菜单栏应用，主要功能包括：

- 自动监听文本剪贴板，每 0.5 秒检测一次系统剪贴板变化。
- 将新的剪贴板内容保存到本地 SQLite 数据库。
- 对重复内容做去重，避免同一内容反复进入历史列表。
- 按内容自动分类，包括链接、邮件、代码、数字、中文、英文、中英混合和其他。
- 提供菜单栏入口，点击图标可以打开剪贴板历史面板。
- 支持全局快捷键呼出面板，默认快捷键是 `⌘⇧V`。
- 提供搜索栏，可以按文本内容快速检索历史记录。
- 提供分类侧边栏，可以按分类筛选剪贴板内容。
- 支持收藏重要记录，收藏内容不会被自动清理。
- 支持清空非收藏历史。
- 支持设置保留天数，自动删除过期的非收藏记录。
- 接入本地 Ollama，用本地大模型生成摘要和自定义 AI 分类。
- 记录 AI 调用历史，方便查看摘要和分类结果。
- 所有剪贴板数据保存在本机，不上传到云端。

## 3. 如何解决之前提到的问题

针对“复制内容容易被覆盖”的问题，项目通过 `ClipboardMonitor` 持续监听系统剪贴板，一旦检测到新文本，就写入历史记录。这样即使系统剪贴板后来被覆盖，之前的内容仍然可以从 ClipFlow 找回。

针对“历史内容难找”的问题，项目提供了搜索、分类侧边栏和详情预览。用户不用再回到原网页或聊天记录里翻找，而是可以直接在 ClipFlow 中检索。

针对“剪贴板内容混乱”的问题，项目在 `ClipboardItem.categorize` 中用规则识别链接、邮箱、代码、数字、中英文内容，并在界面上显示分类标签。常见内容可以被自动归类，降低查找成本。

针对“重要内容不想被清理”的问题，项目实现了收藏功能。自动清理只删除非收藏记录，收藏内容会保留。

针对“隐私和云端上传风险”的问题，项目使用本地 SQLite 保存数据，并通过本地 Ollama 做 AI 摘要和分类。剪贴板内容不需要发送到第三方云端 API。

针对“长文本需要快速理解”的问题，项目提供 AI 摘要功能，可以把选中的剪贴板内容压缩成一句话，帮助用户快速判断内容价值。

## 4. 用了什么技术和工具

项目使用的主要技术和工具包括：

- Swift：应用主要开发语言。
- SwiftUI：构建剪贴板列表、详情页、设置页等界面。
- AppKit：实现 macOS 菜单栏图标、弹窗、系统应用生命周期等能力。
- NSPasteboard：读取和写入系统剪贴板。
- Timer：定时轮询剪贴板变化。
- SQLite.swift：操作本地 SQLite 数据库。
- UserDefaults：保存快捷键、保留天数、Ollama 地址、模型名、自定义分类和 AI 调用记录。
- HotKey：注册全局快捷键。
- Carbon key code：处理 macOS 快捷键按键编码。
- Ollama API：调用本地大模型进行摘要和自定义分类。
- Xcode / XcodeGen：管理和构建 macOS 应用工程。

## 5. 从这个项目中获得了什么

这个项目的价值不只是做了一个剪贴板工具，还验证了一套本地效率工具的实现方式：

- 理解了 macOS 菜单栏应用的基本结构，包括 `NSStatusItem`、`NSPopover` 和应用生命周期。
- 掌握了系统剪贴板监听、去重、回写剪贴板的完整流程。
- 建立了本地持久化能力，知道如何把临时数据保存到 SQLite。
- 练习了 SwiftUI 和 AppKit 混合开发。
- 验证了本地 AI 能力接入桌面工具的可行性。
- 形成了一个“本地数据 + 本地 AI + 快捷入口”的隐私优先应用模式。

更重要的是，这个项目说明：AI 不一定要做成聊天窗口，也可以嵌入具体工作流，帮助用户整理、分类和理解日常数据。

## 6. 做到之后有什么成果和影响

项目完成后，可以带来这些直接成果：

- 用户可以找回过去复制过的文本，减少重复查找。
- 常用链接、代码片段、邮件地址和文字内容可以快速复用。
- 剪贴板历史有分类和搜索，不再是一堆无序文本。
- 重要内容可以收藏，避免被自动清理。
- 长文本可以用本地 AI 快速摘要，提高判断速度。
- 整个数据链路主要在本机完成，降低隐私泄露风险。

从影响上看，ClipFlow 把剪贴板从一次性工具变成了一个轻量知识入口。它适合程序员、学生、写作者、运营人员等频繁复制文本的人群，能减少上下文切换，提高日常操作效率。

## 7. 之后怎么改进

现在项目已经具备核心功能，但还有几个必须正视的改进方向：

- 修复删除按钮逻辑：`DetailView` 里的“删除”按钮当前调用的是摘要更新回调，不是真正删除记录，这会导致用户误操作无效。
- 修复快捷键录制监听：设置页每次录制快捷键都会添加本地事件监听，但没有保存并移除监听对象，长期使用可能造成重复监听。
- 补齐图片和文件剪贴板支持：模型里有 `image` 和 `file` 类型，但当前监听主要处理文本。
- 加强隐私保护：增加敏感内容识别，例如密码、验证码、密钥、银行卡号，并允许自动忽略或快速清除。
- 增加权限和异常提示：当剪贴板读取、数据库写入或 Ollama 连接失败时，界面应该明确提示原因。
- 优化 AI 分类：当前 AI 分类依赖手动点击触发，可以考虑后台异步分类，但要避免影响性能和隐私。
- 增加导入导出：支持导出历史记录或备份数据库，方便迁移设备。
- 增加测试：为分类规则、数据库迁移、去重、保留天数清理和快捷键配置补充单元测试。
- 优化性能：当剪贴板历史很多时，需要分页、虚拟列表或数量上限，避免界面加载变慢。
- 完善发布流程：补齐签名、公证、DMG 打包和自动更新，让项目从本地工程变成真正可分发应用。

## 8. 总结

ClipFlow 解决的是一个很真实的小问题：复制内容经常临时、杂乱、易丢。项目通过 macOS 菜单栏入口、本地剪贴板监听、SQLite 持久化、分类搜索、收藏机制和本地 Ollama AI，把这个小问题做成了一个完整的效率工具。

下一步不应该盲目加功能，而应该先修复已发现的逻辑问题，再补测试和发布流程。否则功能看起来很多，但可靠性还不够硬。
